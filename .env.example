# ── GitHub / 仓库里只保留本文件（无真实密钥）──
#
# 本地开发（不要提交密钥）：
#   cp .env.example .env.local
#   编辑 .env.local，填入 MATRIX_API_KEY=...
#   .env.local 已在 .gitignore 中
#
# 线上（Vercel）：
#   Project → Settings → Environment Variables → MATRIX_API_KEY

MATRIX_API_KEY=

# Optional alias:
# MAISONLOOKS_API_KEY=
